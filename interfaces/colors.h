#pragma once

#include <cstdint>
#include <iomanip>

/*
 *
 *
 *
 *
 *
 * @note may probably delete this, might not needed.
 * @note was deciding what to do with this if we should have colors by default where users can simply call this.
 * @note but am still deciding if this is even needed or we want this to be available to users...
 *
 *
 *
 *
 *
 *
 * */

namespace Colors{
	/* struct Color{ */
	/* 	uint8_t r, g, b, a; */
	/* }; */
	
	/* Color red = {255, 0, 0}; */
	/* Color blue = {0, 0, 255}; */
	/* Color green = {0, 255, 0}; */
};
